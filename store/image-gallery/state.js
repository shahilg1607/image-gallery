export default () => ({
  hasSidePanel: false, // boolean to show and hide the right pane
  selectedImages: [], // Array of object the stores the selected images
  selectedPresentation: [], // Array of object that store the selected presentation type
});
